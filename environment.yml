channels:
  - pytorch # I still prefer to install pytorch via conda even though it will be discontinued in the future
  - nvidia
  - conda-forge
dependencies:
  - python=3.11.*
  - ipykernel=6.29.*
  - pytorch=2.4.* # =*cuda* # Might need to specifically request cuda version
  - torchvision=0.19.*
  - pip>=23
